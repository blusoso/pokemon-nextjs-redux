import { Skeleton } from "@mui/material";
import styled from "styled-components";

export const SkeletonImageStyled = styled(Skeleton)`
  position: absolute;
  top: -2%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
